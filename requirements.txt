# VocabReelGenerator — Python dependencies
# Install with:  pip install -r requirements.txt

# Image generation
Pillow>=9.5.0

# Text-to-speech (calls Google's TTS service — requires internet)
gTTS>=2.3.2

# Audio manipulation (concatenation, silence insertion)
pydub>=0.25.1

# Correct Arabic rendering in PIL (reshaping + RTL direction)
arabic-reshaper>=3.0.0
python-bidi>=0.4.2

# YouTube Data API v3
google-api-python-client>=2.100.0
google-auth-httplib2>=0.1.1
google-auth-oauthlib>=1.1.0
